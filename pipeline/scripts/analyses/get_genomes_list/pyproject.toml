[project]
name = "get-genomes-list"
version = "0.1.0"
description = "Get the assemblies associated to a taxon"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "snakemake>=9.19.0",
]
