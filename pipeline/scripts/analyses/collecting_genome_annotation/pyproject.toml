[project]
name = "collecting-genome-annotation"
version = "0.1.0"
description = "Collecting genomes annotations and sequences"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.2",
    "snakemake>=9.19.0",
]
